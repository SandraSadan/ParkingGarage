export const environment = {
  production: true,
  payPerHour: 60,  // Add production-specific payment rate (e.g. 60 Rs per hour)
  payPerMin: 15    // Add production-specific payment rate (e.g. 15 Rs per minute)
};
